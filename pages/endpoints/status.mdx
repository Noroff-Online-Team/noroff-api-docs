import Callout from 'nextra-theme-docs/callout'

# Status

<Callout>
  This is the API health check endpoint. You will not make use of this in
  assignments.
</Callout>

##### GET /status

> This will always return a 200 OK status code along with a status message.

```json
{
  "status": "OK"
}
```
